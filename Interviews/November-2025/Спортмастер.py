# 1
# Вопрос на собеседовании: чем отличаются .loc и .iloc у DataFrame в pandas?
#
# Задача: объяснить разницу и привести по одному примеру для каждого варианта индексирования.

В **Pandas** метод `loc` выбирает данные **по меткам** строк и столбцов, а `iloc` — **по числовым индексам**.
При срезах (`:`) в `loc` **последний элемент включается**, а в `iloc` — **нет**.
Иными словами, `loc` — по именам, `iloc` — по позициям.


# 2
# Задача: рассказать, чем отличаются операции:
#
# list.remove(x);
# list.pop(i);
# del list[i].
# Нужно пояснить, что они удаляют (по индексу или по значению), что возвращают и как ведут себя при ошибках.

| Операция      | Удаляет            | Возвращает        | Ошибка       |
| ------------- | ------------------ | ----------------- | ------------ |
| `remove(x)`   | по значению        | `None`            | `ValueError` |
| `pop(i)`      | по индексу         | удалённый элемент | `IndexError` |
| `del list[i]` | по индексу / срезу | ничего            | `IndexError` |





# 2.3. append и extend для списков

my_list.append() метод, добавляет элемент (или объект как единый элемент) в конец списка
a = [1, 2]
a.append([3, 4])
print(a)  # [1, 2, [3, 4]]

my_list1.extend(my_list2) метод, расширяет список my_list1, добавляя каждый элемент из переданного итерируемого объекта my_list2. Это может быть строка, не обязательно список. Можно указать индекс, с которого будет добавляться второй список.
Если добавляем строку, то разворачивает и добавляет по одному элементу строки.

# Расширяем список строкой (тк она итерируема)
my_list1 = [1, 2, 3]
my_list1.extend('abc')
print(my_list1)    # [1, 2, 3, 'a', 'b', 'c']


a = [1, 2]
a.extend([5, 6])  # -> [1, 2, 5, 6]


# 3.2. groupby и NaN (dropna)
# Есть DataFrame с одним столбцом, содержащим уникальные значения от 1 до 10, и ещё одной строкой, где значение этого столбца равно NaN.
# Задача:
# Если сделать groupby по этому столбцу с настройками по умолчанию, сколько групп получится и почему?
По умолчанию groupby отбрасывает NaN-ключи (они не образуют группу). Будет 10 групп — по значениям 1..10.

# Как изменить вызов groupby, чтобы добавить отдельную группу для значения NaN?
Указать dropna=False, Тогда будет 11 групп: 1..10 и отдельная группа для NaN.

g = df.groupby("col", dropna=False)



# Задача:
#
# Дать определение нормального распределения и описать его основные параметры.
Нормальное распределение — это тип теоретического распределения, в котором значения в основном сосредоточены вокруг среднего. Это распределение (Функция плотности вероятности для нормального распределения ) имеет форму колокола и описывается двумя параметрами: средним значением = мат ожиданию = μ и дисперсией = σ2.
# Перечислить способы проверки данных на нормальность (визуальные и формальные тесты).
- **Гистограмма + кривая N(μ^,σ^)**: быстрый взгляд на симметрию/многомодальность/тяжёлые хвосты.
- **QQ-plot (квантиль-квантиль):** точки должны лежать почти на прямой; изгибы — скос/тяжёлые хвосты.
- **Критерий согласия Kolmogorov–Smirnov** c поправкой **Лиллиефорса**(когда μ,σ оценены из данных).




# Задача: перечислить основные условия применения t-теста для сравнения средних:
#
# требования к распределению;
# независимость наблюдений;
# ограничения по дисперсиям и объёму выборок;
# что делать, если условия нарушены.

требования к распределению: нормальное распределение

независимость наблюдений: Внутри группы и между группами наблюдения независимы

ограничения по дисперсиям и объёму выборок: дисперсии в группах равны (гомоскедастичность) и объёмы сопоставимы.

что делать, если условия нарушены:

- Неравные дисперсии , Две независимые группы, «обычная» метрика → **Welch t-тест** (по умолчанию).
- Те же люди до/после → **парный t** (проверять нормальность **разностей**).
- Малые n / сильная ненормальность / много выбросов → **Манна–Уитни** (независимые группы) / **Wilcoxon signed-rank (парные группы)**.
- Зависимость/кластеры  → Нужно учесть ковариаты → линейная модель (ANCOVA/OLS) с робастными SE.




# 5.1. Метрики кролиководческой фермы
#
# Представьте, что у вас есть кролиководческая ферма — это ваш продукт и бизнес одновременно. Вам нужно решить, на какие показатели смотреть, чтобы понимать, как идут дела.
#
# Задача:
#
# Перечислить возможные метрики (структура поголовья, смертность, расходы, выручка, маржа, каналы сбыта и т.п.).
# Выбрать только три ключевые метрики, на которые вы бы смотрели, если вас ночью разбудить и спросить: «Ну что, как дела у бизнеса?» — и обосновать выбор.

- **Валовая маржа (₽) за последние 7/30 дней -** Показывает, зарабатываем ли мы сверх прямых затрат.
- **Смертность (падёж) по ключевым возрастным группам (%) -** Ранний индикатор проблем со здоровьем/условиями. Рост на пару п.п. съест всю прибыль
- **Конверсия корма: FCR (кг корма / кг прироста)** Главный драйвер себестоимости. Ухудшение FCR напрямую увеличивает стоимость килограмма мяса и давит маржу.







# 6.1. Задача с таблетками
#
# Пете выдали 4 таблетки: 2 таблетки «Альфа» и 2 таблетки «Бета». Они полностью неотличимы (одинаковые по цвету, форме, запаху и т.п.).
#
# В инструкции написано: сегодня нужно выпить 1 таблетку «Альфа» и 1 таблетку «Бета», и завтра — также 1 «Альфа» и 1 «Бета».
#
# Петя уже дома, а таблетки никак не помечены. Делиться и смешивать таблетки (делить пополам, делать «микстуру») можно, но как-то менять сам состав таблеток — нельзя.
#
# Задача: каким способом Петя может выполнить предписание — выпить сегодня и завтра по одной таблетке каждого вида, если изначально таблетки не отличимы?

Развести все таблетки  В 1 день выпить половину и во 2й день выпить половину.














#